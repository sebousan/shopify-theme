{%- liquid
  assign src = img.preview_image
  assign width = width
  assign height = width | divided_by: src.aspect_ratio | ceil
-%}

<picture>
  <img src="{{ src | image_url: width: width }}" width="{{ width }}" height="{{ height }}" alt="{{ img.alt | escape }}" loading="lazy">
</picture>