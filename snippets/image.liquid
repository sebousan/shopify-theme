{%- liquid
  assign width = 450
  assign height = width | divided_by: img.preview_image.aspect_ratio | ceil
-%}

<picture>
  <img src="{{ img | image_url: width: 450 }}" width="{{ width }}" height="{{ height }}" alt="{{ img.alt | escape }}" loading="lazy">
</picture>