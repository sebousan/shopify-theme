{%- liquid
  assign width = 400
  assign height = width | divided_by: img.preview_image.aspect_ratio | ceil
-%}

<picture>
  <img src="{{ img.preview_image | image_url: width: width }}" width="{{ width }}" height="{{ height }}" alt="{{ img.alt | escape }}" loading="lazy">
</picture>