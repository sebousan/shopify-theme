{% liquid 
  assign title = section.settings.title
  assign subtitle = section.settings.subtitle
%}

<div class="container">
  {% render 'block-heading', title: title, subtitle: subtitle %}

</div>

{% schema %}
  {
    "name": "t:sections.editorial-column.name",
    "tag": "section",
    "class": "block-editorial-column",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "t:sections.global.title"
      }, {
        "type": "richtext",
        "id": "subtite",
        "label": "t:sections.global.subtitle"
      }
    ],
    "max_blocks": 5,
    "blocks": [
      {
        "name": "Item",
        "type": "t:sections.global.item",
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "t:sections.global.title"
          }, {
            "type": "richtext",
            "id": "subtite",
            "label": "t:sections.global.subtitle"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "t:sections.editorial-column.name"
      }
    ]
  }
{% endschema %}